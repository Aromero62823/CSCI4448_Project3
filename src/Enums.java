public class Enums {
    public enum staffType {Intern, Mechanic, Salesperson}
    public enum vehicleType {PerformanceCar, Car, Pickup}
    public enum vehicleCondition {New, Used, Broken}
    public enum vehicleCleanliness {Sparkling, Clean, Dirty}
}
